name: neshan_maps_flutter
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.6.0

  latlong2: ^0.9.1

  webview_flutter: ^4.13.0
  web: ^1.1.1
  geolocator: ^13.0.4
  pointer_interceptor: ^0.10.1+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

  assets:
    - assets/html/
